# src/

One optimized SVG per icon, named by id (`passport.svg`, `ward.svg`). Must follow the iconography-style chapter: grid, stroke weight, corner radius, filled/outline rules. Run svgo before committing.
