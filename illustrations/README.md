# illustrations/

`export/` = small web-ready SVGs (in git). `meta/` = per-asset YAML: title/title_ne, license, status (official|community|under-review), contributor; large editable sources live in object storage via storage_key + sha256 checksum.
